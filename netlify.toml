[build]
  command = "cargo build --release && ./target/release/folio build"
  publish = "dist"
 
[build.environment]
  # Netlify persists /opt/buildhome/.cargo between builds automatically
  # which caches downloaded crates and avoids re-fetching on every deploy
  CARGO_HOME = "/opt/buildhome/.cargo"
 
